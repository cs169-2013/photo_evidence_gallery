<%= form_tag({:action => :import}, multipart: true) do %>
  <%= file_field_tag :csv_file %>
  <%= submit_tag( "Import" ) %>
<% end %>

<% if !flash.empty? %>
  <div id="flash"> 
      <% flash.keys.each do |k| %> 
          <div class="<%= k %>">
              <%= flash[k] %>
          </div>  
      <% end %>   
  </div>
<% end %>  